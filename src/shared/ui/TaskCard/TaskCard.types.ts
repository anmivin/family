import { components } from '@shared/api/Api';

export const taskType = ({
  isActive,
  isCompleted,
  isApproving,
  isDeclined,
  isHabit,
}: {
  isActive?: boolean;
  isCompleted?: boolean;
  isApproving?: boolean;
  isDeclined?: boolean;
  isHabit?: boolean;
}) => {};
